apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: searxng-data
  name: searxng-data
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
