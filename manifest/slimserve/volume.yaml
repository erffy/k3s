apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: slimserve-data
  name: slimserve-data
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
